'use client';
import {useState} from 'react';
const tools=[['💡','Startup Idea Generator','Generate practical startup ideas from your skills, budget and market.'],['📋','Business Plan','Create a clear business model, target customer and execution roadmap.'],['📈','Market Analyzer','Analyze customers, competitors, positioning and opportunities.'],['💰','Revenue Planner','Explore pricing, revenue streams and simple growth targets.'],['📣','Marketing Plan','Generate a launch and customer-acquisition strategy.']];
export default function Home(){const [prompt,setPrompt]=useState('');const [result,setResult]=useState('');const [loading,setLoading]=useState(false);function generate(){if(!prompt.trim())return;setLoading(true);setTimeout(()=>{setResult(`Startup AI Analysis\n\nIdea: ${prompt}\n\nTarget customers: Focus on a specific customer segment with a frequent problem.\n\nBusiness model: Start with a simple paid plan and validate demand before scaling.\n\nLaunch plan: Validate 10 customers → build MVP → launch → measure conversion → scale the best channel.\n\nDemo mode: connect a server-side AI API to enable live AI generation.`);setLoading(false)},700)}return <main><nav><div className="logo">✦ Startup<span>AI</span></div><button>Sign in</button></nav><section className="hero"><div className="badge">🚀 Build your next startup</div><h1>Turn your idea into a <span>real business.</span></h1><p>AI-powered startup planning for ideas, customers, revenue and growth — in English, Hindi or Hinglish.</p><div className="box"><textarea value={prompt} onChange={e=>setPrompt(e.target.value)} placeholder="Describe your startup idea, skill, budget or problem..."/><button className="primary" onClick={generate}>{loading?'Generating...':'Generate with AI →'}</button></div>{result&&<pre className="result">{result}</pre>}</section><section className="tools"><h2>Everything you need to launch</h2><div className="grid">{tools.map(([i,t,d])=><article key={t}><b>{i}</b><h3>{t}</h3><p>{d}</p><button onClick={()=>setPrompt(t)}>Try it →</button></article>)}</div></section><footer>© 2026 StartupAI · AI planning platform · Razorpay-ready</footer></main>}
